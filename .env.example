# SYR Environment Configuration

# Application
NODE_ENV=development
PORT=5173
PUBLIC_URL=http://localhost:5173

# Docker User Permissions (use your host UID/GID to avoid permission issues)
# Get your UID/GID with: echo "UID=$(id -u) GID=$(id -g)"
UID=1000
GID=1000

# SurrealDB Configuration
SURREALDB_URL=ws://surrealdb:8000/rpc
SURREALDB_NAMESPACE=syr
SURREALDB_DATABASE=syr
SURREALDB_USER=root
SURREALDB_PASS=syr-dev-password

# SeaweedFS (S3) Configuration
S3_ENDPOINT=http://seaweedfs:8333
S3_ACCESS_KEY_ID=syr-access-key
S3_SECRET_ACCESS_KEY=syr-secret-key
S3_BUCKET=syr-storage
S3_REGION=us-east-1
S3_PUBLIC_URL=http://localhost:8333

# JWT Configuration
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# OAuth Configuration
OAUTH_ISSUER=http://localhost:5173
OAUTH_AUTH_CODE_EXPIRES_IN=600
OAUTH_ACCESS_TOKEN_EXPIRES_IN=3600
OAUTH_REFRESH_TOKEN_EXPIRES_IN=2592000

# DID Configuration (did:web)
DID_WEB_DOMAIN=localhost:5173
DID_WEB_PATH_PREFIX=users

# Password Hashing (Argon2id)
# Memory cost in KiB (64 MiB recommended)
ARGON2_MEMORY_COST=65536
# Time cost (number of iterations)
ARGON2_TIME_COST=3
# Parallelism (number of threads)
ARGON2_PARALLELISM=4

# Security
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# CORS
CORS_ORIGIN=http://localhost:5173
CORS_CREDENTIALS=true

# MCP Servers (optional - for development tooling)
# Start with: docker-compose --profile mcp up
# SurrealMCP will be available at http://localhost:8090/mcp
MCP_ENABLED=true

