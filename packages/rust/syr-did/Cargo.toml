[package]
name = "syr-did"
version = "0.1.0"
edition = "2021"
description = "did:syr parse, document, and validate"
license = "MIT"

[dependencies]
syr-crypto-core = { path = "../syr-crypto-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
once_cell = "1"
