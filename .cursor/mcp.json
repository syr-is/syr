{
  "mcpServers": {
    "SurrealDB": {
      "url": "http://localhost:8090/mcp",
      "description": "Connects to SurrealMCP running in docker-compose (port 8090). Start with: docker-compose --profile mcp up"
    },
    "svelte": {
      "command": "npx",
      "args": ["-y", "@sveltejs/mcp"],
      "description": "Svelte MCP server for component and route management"
    }
  }
}
