{
  "mcpServers": {
    "svelte": {
      "command": "npx",
      "args": ["-y", "@sveltejs/mcp"],
      "description": "Svelte MCP server for component and route management"
    },
    "SurrealDB": {
      "url": "http://localhost:8090/mcp",
      "description": "SurrealDB MCP – query and manage SurrealDB (Docker Compose)"
    }
  }
}
